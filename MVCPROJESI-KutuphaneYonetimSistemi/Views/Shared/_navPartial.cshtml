<nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">Imladris Kütüphanesi</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav me-auto">
                @if (User.Identity.IsAuthenticated)
                {
                    <a class="nav-link" asp-action="List" asp-controller="Book">Kitaplar</a>
                    <a class="nav-link" asp-action="List" asp-controller="Author">Yazarlar</a>
                    <a class="nav-link" asp-action="About" asp-controller="Home">Hakkında</a>
                }
            </div>
            <div class="navbar-nav ms-auto">
                @if (User.Identity.IsAuthenticated)
                {
                    <span class="navbar-text">
                        Hoş geldiniz, @User.Identity.Name!
                    </span>

                    <form asp-controller="Auth" asp-action="Logout" method="post" class="d-inline">
                        @Html.AntiForgeryToken()
                        <button type="submit" class="btn btn-link">Çıkış Yap</button>
                    </form>
                }
                else
                {
                    <a class="nav-link" asp-controller="Auth" asp-action="Login">Giriş Yap</a>
                    <a class="nav-link" asp-controller="Auth" asp-action="SignUp">Kayıt Ol</a>
                }
            </div>
        </div>
    </div>
</nav>
